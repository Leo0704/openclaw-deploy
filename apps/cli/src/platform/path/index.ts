/**
 * 路径模块
 */

export { normalizePath, normalizeProjectPath } from './platform-paths';
export { getDefaultInstallPath, scanInstallPaths, scanUserDirectories } from './platform-paths';
export { validatePath } from './platform-paths';
export { readJsonFile } from './platform-paths';
export { isOpenClawProject, isOpenClawProjectDir } from './platform-paths';
